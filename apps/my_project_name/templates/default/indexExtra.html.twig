{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name}}!{% endblock %}

{% set users = [{name:"John"}, {name: "Susan"}, {name: "Robert"}] %}
{% set string = "some string \n from \n database "%}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name|upper }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/home/dragos/courses/udemy/symfony_4_5/apps/my_project_name/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/dragos/courses/udemy/symfony_4_5/apps/my_project_name/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code></li>
    </ul>

    <ul>
        {% for user in users %}
            <p>
                {{ user.name }} 
            </p>
        {% endfor%}

    </ul>

    {% for message in app.flashes('notice')%}
        <div class="flash-message">
            {{ message }}
        </div>
    {% endfor%}

    {% for label, messages in app.flashes %}

        {% for message in messages %}
            <div class="flash-{{ label }}">
                {{ message }}
            </div>
        {% endfor%}
    {% endfor%}
</div>

{{ include('includes/subview.html.twig', {users:"users"})}}
{{ path('home')}}
<br>
{{ url('home')}}
<br>
<p>
    {{ga_code}}
</p>


{{render(controller(
    'App\\Controller\\DefaultController::mostPopularPosts',
    { 'number' : 5}
))}}

 
{% endblock %}
 {% block javascripts %}
    <script>
        var route = "{{ string|escape('js') }}";
        alert(route);
    </script>
 {% endblock %}